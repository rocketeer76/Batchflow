# Batchflow
An asynchronous CSV import tool built with Astro, SolidJS, TypeScript and Neon Postgres. Upload files, map columns, validate rows, process imports in the background, track progress and review downloadable row-level errors.
